在 worktree C:\code\Hermes-Boris-game-wt-sprint1（分支 feat/sprint-week1-d1）做 D1-5。

# 任务：切调色板到 tokyo_heisei

1. 新建 `assets/palette-tokyo-heisei.json`（6 主色：暗紫 #2d1b3d / 酒红 #8b2942 / 和风米 #d4c5a0 / 暮蓝 #4a5a7a / 烛黄 #d4a04a / 黑 #0a0a14）
2. 改 `assets/palette.json`：加 `chapter` 字段（参考 `docs/dev-plan-full.md` 第 2.5 节）
3. 改 `src/render/palette.ts`：能按 chapter 选调色板
4. 跑 `pnpm typecheck && pnpm test`，全过
5. 1 个 commit：`feat: 切调色板到 tokyo_heisei (D1-5)`

# 已知陷阱（CLAUDE.md 17+1 条）
- pnpm only（不要 npm/yarn）
- biome 不用 `[process.env.FOO]`，用 `process.env.FOO`
- vitest happy-dom 不实现 Canvas，必须 Proxy mock
- `ctx.input` 类型要扩成 `MockInput`（不是 `InputSnapshot`）
- 改完跑一次 `pnpm test` 看 baseline

# 完事汇报
- commit hash
- 改的文件清单
- `pnpm test` 输出最后 5 行
- 遇到什么问题（如有）
