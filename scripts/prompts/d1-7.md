# Hermes-Boris-Game — D1-7 Task: 修 sprint 草稿偏差 #2 + #3

> **你的身份**：5 tab 协作模式中的 **Tab 2（Debug）**。orchestrator 主 session 派你做 D1-7（修已知问题）。
> 这是一个**全新独立上下文**——你**没有**主 session 的 memory。下面是完整已知信息。

---

## 0. 工作环境

- **工作目录**：`C:\code\Hermes-Boris-game-wt-sprint1`
- **分支**：`feat/sprint-week1-d1`（base = main b2692c3）
- **HEAD 已有的 commits**（按时间序）：
  - `4bd953c` docs: world-bible.md
  - `cde15a6` chore: CLAUDE.md 主题约束
  - `124a69d` chore: scripts for parallel tab dispatch
- **协作**：同 worktree 上还有 **Tab 1** 在并行做 D1-5（调色板），**改的文件不重叠**（你碰 `src/renderer/index.html` 或 CSS + `package.json`；Tab 1 碰 `assets/palette*` 和 `src/render/palette.ts`）。两个 tab 都 commit 到同一分支，最后由 orchestrator 整理 PR。

---

## 1. 项目 1 句话

对标《黑森町绮谭》的 16-bit 像素视觉小说（VN），4-5h 单线剧情。主角被吸入"黑森町"虚构小镇（飘忽 1980s 平成泡沫经济余韵），解 3 段怪谈（鹿骨 / 列车 / 映画）+ 序章 + 尾声，2 结局（普通 + 真）。

**当前 sprint-week1 目标** = MVP = 序章 + Ch.1 鹿骨怪谈 = 约 90 分钟可玩。
**完整版** = 序 + 3 章 + 尾声 = 4-5h。

---

## 2. 技术栈

- **Electron 30+ + electron-vite + TypeScript**
- Canvas 2D 渲染 480×270 内部
- Vitest（happy-dom）+ Playwright（E2E + 视觉回归）
- Biome（lint + format）
- **pnpm** —— 唯一包管理器（**不要 npm/yarn**）

---

## 3. 已知陷阱（CLAUDE.md 第 95-114 行，全 17+1 条必读）

完整列表见工作目录的 `CLAUDE.md`。这里只列**对 D1-7 相关的**：

- **pnpm only**（不要 npm/yarn）
- **⭐ electron-builder 25 拉了 `app-builder-bin@5.0.0-alpha.10`，Windows 上 `ERR_ELECTRON_BUILDER_CANNOT_EXECUTE`** → **降级到 24.x**（这正是你要修的偏差 #3，**根因已知**）
- electron-builder 24 跟 electron 30+ 兼容性 OK，先看 `pnpm-lock.yaml` 确认
- electron 二进制国内 `ECONNRESET` → `.npmrc` 加 `electron_mirror=https://npmmirror.com/mirrors/electron/`
- pnpm 11 native 包（electron、esbuild、@biomejs/biome）postinstall 要 `allowBuilds` 批准
- 改 `package.json` 后**必须** `pnpm install` 一次（不跑锁文件不更新，下次别人 `pnpm i` 会装回 25）
- biome `lint/complexity/useLiteralKeys` 禁 `process.env['FOO']`
- `.claude/**` 加到 `biome.json#files.ignore`（CRLF 不被 biome format 改，CI 必挂）
- vitest happy-dom 15 不实现 `CanvasRenderingContext2D` → Proxy mock
- CI autocrlf no-op：`actions/checkout@v4` 的 `autocrlf: false` 是 no-op；要真控 line ending 用 `.gitattributes`（`* text=auto eol=lf`）

---

## 4. 项目结构（关键路径）

```
Hermes-Boris-game/
├── src/renderer/
│   ├── index.html          # ⚠️ 标题屏可能在这
│   └── src/                # 渲染逻辑（可能 CSS 散落在 .ts）
├── package.json            # ⚠️ electron-builder 版本在这
├── electron-builder.yml    # 打包配置
└── CLAUDE.md
```

---

## 5. 任务 D1-7：修 sprint 草稿偏差 #2 + #3

### 5.1 偏差 #2：标题屏 flex 居中

- **症状**：标题屏（游戏启动第一个画面）没居中
- **可能位置**：`src/renderer/index.html` 或 `src/renderer/src/` 下的 CSS / 内联 style
- **定位**：先 `grep -r "title" src/renderer/` 找标题屏的容器
- **修法**：确保容器有：
  ```css
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  ```
- **验证**：`pnpm dev` 启动后看标题屏是不是真的居中（截图 or 描述）

### 5.2 偏差 #3：electron-builder 降级到 24

- **症状**：`pnpm build` 跑 electron-builder 时 `process failed ERR_ELECTRON_BUILDER_CANNOT_EXECUTE`
- **根因**（CLAUDE.md 17+1 已知陷阱）：electron-builder 25 拉 `app-builder-bin@5.0.0-alpha.10`，Windows pack 失败
- **修法**：
  1. `package.json` 把 `electron-builder` 从 `^25.x` 改成 `^24.x`（**注意是 devDependencies**）
  2. **必须** 跑 `pnpm install`（更新 lockfile）
  3. 跑 `pnpm build` 验证 `dist/` 真的产出
- **验证**：`pnpm build` 退出码 0 + `dist/` 有产物

### 5.3 完整步骤

1. **先看现状**（避免盲改）：
   - `cat package.json | grep -A1 electron-builder`
   - `cat src/renderer/index.html`
   - `grep -r "title\|splash" src/renderer/` 找标题屏在哪
   - 记录 baseline `pnpm test` 输出

2. **修偏差 #2**（标题屏 flex 居中）：
   - 改 CSS / HTML
   - 1 个 commit：`fix: 标题屏 flex 居中 (D1-7)`

3. **修偏差 #3**（electron-builder）：
   - 改 `package.json`
   - `pnpm install`
   - 1 个 commit：`chore: electron-builder 降级 24.x (D1-7)`

4. **跑门禁**：
   - `pnpm typecheck && pnpm test` 全过
   - `pnpm build` 成功 + `dist/` 有产物
   - `pnpm dev` 启动看标题屏居中

### 5.4 验证清单

- [ ] 标题屏 flex 居中（截图 / 视觉确认）
- [ ] `electron-builder` 改为 `^24.x`
- [ ] `pnpm install` 成功 + lockfile 更新
- [ ] `pnpm build` 成功 + `dist/` 有产物
- [ ] `pnpm typecheck && pnpm test` 全过
- [ ] 2 个 commit（fix + chore）

---

## 6. 报给 orchestrator

完成后回复（**贴完整输出**）：

- 2 个 commit hash
- 改动的文件清单
- `pnpm typecheck` 输出最后 5 行
- `pnpm test` 输出最后 10 行
- `pnpm build` 输出最后 10 行
- 标题屏居中确认（截图 if possible）
- 遇到什么问题（如有）

---

## 7. 协调（硬规则）

- ❌ **不直接 push** —— orchestrator 统一合（squash merge）
- ❌ **不直接 merge main**
- ❌ **不修改 `CLAUDE.md`** —— orchestrator 的活
- ❌ **不修改 `scripts/`** —— orchestrator 的活
- ❌ **不修改 `assets/palette*` 或 `src/render/palette.ts`** —— Tab 1 在做
- ✅ **不攒 commit** —— 每个偏差 1 个 commit
- ✅ **遇到歧义问 orchestrator**，别猜
- ✅ **跑 build / test 失败先看 CLAUDE.md 陷阱**再修
- ✅ **降级后必须 `pnpm install`**——不跑 install 锁文件不更新，下次别人 `pnpm i` 会装回 25
- ✅ **baseline 不过的 test 先报告**别硬改
