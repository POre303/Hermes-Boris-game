在 worktree C:\code\Hermes-Boris-game-wt-sprint1（分支 feat/sprint-week1-d1）做 D1-7。

# 任务：修 sprint 草稿偏差 #2 + #3

## 偏差 #2：标题屏 flex 居中
- 修 `src/renderer/index.html` 或对应 CSS：`display: flex; align-items: center; justify-content: center; min-height: 100vh`

## 偏差 #3：electron-builder 降级到 24
- 修 `package.json`：`electron-builder` 25.x → 24.x
- `pnpm install` 一次
- 验证：`pnpm build` 真的产出 `dist/`

# 验证
1. `pnpm typecheck && pnpm test` 全过
2. `pnpm build` 成功
3. `pnpm dev` 启动看标题屏居中

# Commit（2 个）
- `fix: 标题屏 flex 居中 (D1-7)`
- `chore: electron-builder 降级 24.x (D1-7)`

# 已知陷阱
- pnpm only
- electron-builder 24 跟 electron 30+ 兼容性先看 lockfile
- 别忘 `pnpm install`

# 完事汇报
- 2 个 commit hash
- `pnpm build` 最后 5 行
- `pnpm test` 最后 5 行
- 标题屏居中截图（如能截）
